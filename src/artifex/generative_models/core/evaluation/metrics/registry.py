"""Registry ownership lives in calibrax.metrics.

Use ``calibrax.metrics.MetricRegistry`` directly for registry-backed metric
lookup and composition. Artifex no longer ships a parallel local registry.
"""
