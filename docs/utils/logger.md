# Logger

**Status:** `Supported runtime utility`
**Module:** `artifex.generative_models.utils.logging.logger`
**Source:** `src/artifex/generative_models/utils/logging/logger.py`

This page documents the retained logging base classes and concrete built-in
logger implementations used by generative-model workflows.

## Key Symbols

- `Logger`
- `ConsoleLogger`
- `FileLogger`
- `create_logger(...)`

## Current Scope

These logging helpers are package-local utilities for the generative-model
stack rather than top-level `artifex.utils.logging` owners.
